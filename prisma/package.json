{
  "name": "algopractice-database",
  "version": "1.0.0",
  "description": "Database migration and setup for Algorithm Practice app",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts",
    "db:reset": "prisma migrate reset"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "tsx": "^4.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  }
}
